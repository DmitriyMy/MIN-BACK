export const CHAT_QUEUE = 'Chat'

export enum chatStatus {
    sent = 'sent',
    delivered = 'delivered',
    read = 'read',
}
export enum ChatType {
  PRIVATE = 'private',
  GROUP = 'group',
  CHANNEL = 'channel'

}

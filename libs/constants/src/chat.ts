export const CHAT_QUEUE = 'chat'

export enum ChatType {
  PRIVATE = 121,
  PRIVATE_GROUP = 128,
  GROUP = 828,
}
export enum ChatType {
  PRIVATE = 'private',
  GROUP = 'group',
  CHANNEL = 'channel'

}

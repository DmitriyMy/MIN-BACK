export const USER_QUEUE = 'user'

export enum UserRole {
  person = 'person',
  guest = 'guest',
  moderator = 'moderator',
}

export enum UserStatus {
  available = 'available',
  blocked = 'blocked',
}
